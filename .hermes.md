# Hermes Agent — Projects Workspace

## Identity & Scope
- **Agent account**: `ecentella-7x9k2m` (GitHub)
- **Email**: ecentellahermes@gmail.com
- **gh CLI**: `C:/Users/ericc/.local/bin/gh.exe` — use `GH_TOKEN=[REDACTED]`
- **Context**: This repo stores all collaborative projects, tools, and artifacts built with the user.

## Project Organization
- Each project gets its own top-level folder (e.g., `backtest_engine/`, `dashboard/`).
- Every project folder includes: README.md (purpose + usage), main entry file, requirements if applicable.
- Keep folders focused — one concept per directory.

## Git Workflow
1. Always commit with clear messages: `<type>: brief description` (feat:, fix:, chore:, docs:)
2. Push after every meaningful save — this repo is the source of truth for session recovery.
3. No branches needed unless a feature spans multiple sessions; work on main directly.

## Agent Behavior in This Repo
- When creating new projects, write README.md first explaining purpose and how to run it.
- Before pushing, verify files are correct (read back what was written).
- Use `hermes tools` for tool enumeration when unsure of capabilities.
- Reference persistent memory for user preferences (trading focus: crypto swing trades, stocks as leading indicators).

## Key Paths on Disk
- Home: `C:/Users/ericc/`
- Local skills: `$HERMES_HOME/skills/` or `/c/Users/ericc/AppData/Local/hermes/skills/`
- Config: `C:/Users/ericc/AppData/Local/hermes/config.yaml`
- Memory/SOUL: `C:/Users/ericc/AppData/Local/hermes/SOUL.md`

## Notes for Agent Sessions
- This `.hermes.md` loads when running in any subdirectory of this repo.
- For cross-agent compatibility, also maintain an `AGENTS.md` at the root if needed.
- Session history is stored separately; use `session_search` to recall past work.
- Memory tool (2,200 char limit) holds user facts — curator consolidates weekly.
